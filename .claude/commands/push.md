Commit all modified files in bpn-ops and push to the main branch on GitHub, which auto-deploys to https://adityabasak1.github.io/bpn-ops/.

Ask the user for a short commit message if one hasn't been provided, then:
1. Run `git status` to check what's changed
2. `git add` the changed files
3. Commit with the message
4. `git push origin main`

Report the commit hash and confirm the push succeeded.
